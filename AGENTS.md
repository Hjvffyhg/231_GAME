You must ALWAYS track user feature requests and the implementation results in `PROMPTS.md`. 
For every new feature request or significant bug fix requested by the user, append a new section to `PROMPTS.md` with the heading containing the feature name/number, the exact prompt blockquoted, and the result explaining the changes made.

Before implementing any complex feature request, you must automatically draft and refine an execution plan inside `plan.md` first. Ensure visions align perfectly with the user before writing any software implementations or generating component code.

